/** Greet someone. */
export function hello(name: string): string {
  return `Hello ${name}`;
}

/** Farewell message. */
export function goodbye(name: string): string {
  return `Goodbye ${name}`;
}
