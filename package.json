{
  "name": "openpkg",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "bun run build:spec && bun run build:sdk && bun run build:cli",
    "build:spec": "cd packages/doccov-spec && bun run build",
    "build:sdk": "cd packages/sdk && bun run build",
    "build:cli": "cd packages/cli && bun run build",
    "dev": "bun run --parallel dev:spec dev:sdk dev:cli",
    "dev:spec": "cd packages/doccov-spec && bun run dev",
    "dev:sdk": "cd packages/sdk && bun run dev",
    "dev:cli": "cd packages/cli && bun run dev",
    "lint": "bunx biome check packages/",
    "lint:fix": "bunx biome check --write packages/",
    "format": "bunx biome format --write packages/",
    "changeset": "changeset",
    "version": "changeset version && bun install",
    "release": "changeset publish"
  },
  "devDependencies": {
    "@biomejs/biome": "2.1.2",
    "@changesets/cli": "^2.29.7",
    "@types/bun": "latest",
    "@types/node": "^20.0.0"
  },
  "dependencies": {
    "@openpkg-ts/sdk": "file:../openpkg-ts/packages/sdk"
  }
}
