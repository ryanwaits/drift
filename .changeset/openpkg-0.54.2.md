---
"@driftdev/sdk": patch
"@driftdev/cli": patch
---

Bump `@openpkg-ts/sdk` to ^0.54.2: stubbed `typescript` API types carry their package origin, and a platform global's origin no longer depends on TypeScript's declaration order.
