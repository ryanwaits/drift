---
'@driftdev/cli': patch
'@driftdev/sdk': patch
---

Drop leftover health score, coverage-history subpath, sandbox/GitHub URL helpers, and unused CLI progress UI. Coverage floor stays `coverage.min` / `--min`.
