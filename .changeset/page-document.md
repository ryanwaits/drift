---
"@driftdev/sdk": minor
"@driftdev/cli": minor
---

Add `PageDocument`: page-level claims with source locators and spec slices for docs hosts. SDK `buildPageDocument`; CLI `drift page --json`. Scan/lint unchanged.
