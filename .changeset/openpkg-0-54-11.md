---
"@driftdev/sdk": patch
"@driftdev/cli": patch
---

Bump `@openpkg-ts/sdk` to ^0.54.11. `ArrayLike` and `ArrayBufferLike` are inlined instead of emitted as a `$ref` that is never registered. Spec stays ^0.54.9.
