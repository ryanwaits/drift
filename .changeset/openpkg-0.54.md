---
"@driftdev/sdk": patch
"@driftdev/cli": patch
---

Bump `@openpkg-ts/sdk` to ^0.54.0: external type origins resolve to the real package on pnpm and bun installs (was `.pnpm`), and the extractor no longer carries a model dependency.
