---
'@driftdev/sdk': patch
'@driftdev/cli': patch
---

- `spec-not-in-claims`: a section that walks a type's members (three or more distinct public members named by code-span labels, calls on a bound receiver or `Type.member` text) documents its surface and gets its gaps again (regression in 1.19.0 on reference-style guides such as lively's storage page). One example with an option key stays a guide section.
- A migration "before" fence (`Before` / `Previous` / `Old` heading, lower version label, `// before` comment) carries no claims at all, inventory included: the old API is shown on purpose, so no question is asked of it.
